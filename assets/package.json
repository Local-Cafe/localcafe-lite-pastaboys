{
  "name": "local_cafe-assets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "deploy": "node build.js --deploy"
  },
  "devDependencies": {
    "lightningcss": "^1.28.3"
  }
}
